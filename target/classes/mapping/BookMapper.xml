<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.test.app.mapper.BookMapper">
    <!--<insert id="save" parameterMap="User">
        INSERT INTO user_t(user_name,password,age) VALUES (#{userName},#{age})
    </insert>-->

    <select id="selectAllBook" resultType="com.test.app.entity.Book">
        select * from book
    </select>
</mapper>